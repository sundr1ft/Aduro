name: CI

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm

      - run: npm ci
      - run: npm run compile
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run format:check
      - run: npm test
      - run: npm audit --audit-level=moderate

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  dependency-review:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/dependency-review-action@v5

  smoke:
    runs-on: ubuntu-latest
    needs: [check, security]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Pack
        run: mkdir -p /tmp/pkg && npm pack --pack-destination /tmp/pkg

      - name: Install from tarball
        run: |
          mkdir -p /tmp/smoke
          cd /tmp/smoke
          npm init -y
          npm install /tmp/pkg/*.tgz

      - name: CLI responds to --help
        run: /tmp/smoke/node_modules/.bin/aduro --help

      - name: Build example site
        run: /tmp/smoke/node_modules/.bin/aduro build ${{ github.workspace }}/example /tmp/smoke-out

      - name: Verify output
        run: |
          test -f /tmp/smoke-out/index.html
          test -f /tmp/smoke-out/posts/hello-world/index.html
          test -f /tmp/smoke-out/posts/no-date-post/index.html
          test -f /tmp/smoke-out/style.css
          ! test -f /tmp/smoke-out/posts/draft-post/index.html

      - name: Init new site
        run: |
          cd /tmp
          /tmp/smoke/node_modules/.bin/aduro init smoke-site

      - name: Verify scaffold files
        run: |
          test -f /tmp/smoke-site/config.yaml
          test -f /tmp/smoke-site/layouts/index.html
          test -f /tmp/smoke-site/layouts/post.html
          test -f /tmp/smoke-site/layouts/page.html
          test -f /tmp/smoke-site/layouts/partials/header.html
          test -f /tmp/smoke-site/layouts/partials/footer.html
          test -f /tmp/smoke-site/static/style.css
          test -f /tmp/smoke-site/content/about.md

      - name: Build initialised site
        run: /tmp/smoke/node_modules/.bin/aduro build /tmp/smoke-site /tmp/smoke-site-out

      - name: Verify initialised site output
        run: |
          test -f /tmp/smoke-site-out/index.html
          test -f /tmp/smoke-site-out/about/index.html
          test -f /tmp/smoke-site-out/style.css
